<!DOCTYPE html>
<html>
<head>
  <title>Perfil</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <link href="/css/profile.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;700&display=swap" rel="stylesheet">
</head>
<body>
  <%- include('partials/navbar') %>
  <div id="main" class="container profile-container">
    <h1>Perfil</h1>
    <img src="/img/profile_picture.jpg" alt="Foto de Perfil" class="img-thumbnail">
    <p class="mt-3"><i class="fas fa-envelope"></i><strong>Email:</strong> <%= user.email %></p>
    <p><i class="fas fa-id-badge"></i><strong>UID:</strong> <%= user.uid %></p>
    <p><i class="fas fa-check-circle"></i><strong>Email Verificado:</strong> <%= user.emailVerified ? 'Sim' : 'Não' %></p>
    <p><i class="fas fa-user"></i><strong>Nome de Exibição:</strong> <%= user.displayName %></p>
    <button class="btn btn-primary btn-edit-name">Editar Nome</button> <!-- Adicionada a classe btn-edit-name -->
  </div>

  <!-- Modal para editar o nome -->
  <div id="displayNameModal" class="modal">
    <div class="modal-content">
      <span class="close">&times;</span>
      <form action="/profile/update" method="POST">
        <div class="form-group">
          <label for="displayName">Novo Nome de Exibição:</label>
          <input type="text" id="displayName" name="displayName" class="form-control" value="<%= user.displayName %>" required>
        </div>
        <button type="submit" class="btn btn-success">Atualizar Nome</button>
      </form>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script src="/js/script.js"></script>
  <script src="/js/profile.js"></script>
</body>
</html>
